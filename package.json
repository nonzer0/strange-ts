{
  "name": "strange-ts",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "netlify deploy --prod",
    "deploy:dry": "netlify deploy"
  },
  "devDependencies": {
    "@types/node": "^17.0.21",
    "typescript": "^4.5.4",
    "vite": "^2.8.0",
    "netlify-cli": "^9.12.2"
  },
  "dependencies": {
  }
}
